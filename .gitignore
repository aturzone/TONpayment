# Binaries
/bin/
/server
/tonpayment
*.exe
*.test

# Local runtime data (in-memory/JSON store) and secrets
/data/
.env
.env.*
!.env.example
# Keep the no-secret prod template tracked (the compose file references it).
!.env.prod.example

# Go tooling output
*.out
coverage.*

# MASTER CLAUDE tooling + local audit/map output (keep out of the public repo)
/.security/
/.sentinel/
/.master-claude/
/.claude/

# Editor / OS
.DS_Store
.idea/
.vscode/
